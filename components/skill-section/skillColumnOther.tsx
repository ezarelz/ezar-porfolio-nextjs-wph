import { X } from 'lucide-react';

export default function SkillColumnOther() {
  return (
    <div className='size-7 rounded-full bg-[#414651] flex items-center justify-center'>
      <X className='w-4 h-4 text-[#0A0D12]' strokeWidth={2} />
    </div>
  );
}
